# smart-frame 项目说明

生成时间：2026-04-19 18:21（Asia/Shanghai）

## 1. 项目定位

`smart-frame` 是一个面向老人远程看照片场景的智能相框项目，目标是让家人通过微信小程序上传照片，由云端转换为适配电子墨水屏的 BMP 图片，再由 ESP32-S3 相框设备拉取或接收刷新指令后显示。

从当前仓库代码看，这不是单一端项目，而是一个“三端协作”的完整系统：

- `firmware/`：ESP32-S3 固件，负责配网、绑定、拉图/收图、SD 卡落盘、电子墨水屏显示
- `server/`：FastAPI 服务端，负责设备绑定、图片转换、图片存储、状态查询
- `miniprogram/`：微信小程序，负责绑定设备、上传照片、查看设备在线状态

## 2. 当前架构理解

### 2.1 总体链路

```text
微信小程序
  -> 调用 FastAPI 上传照片 / 绑定设备 / 查询状态
  -> 服务端将照片转换为 800x480 24-bit BMP
  -> 服务端按 device_id 保存为 photos/{device_id}/current.bmp
  -> ESP32-S3 通过 HTTP 轮询或 MQTT 触发后下载 BMP
  -> 固件写入 /sdcard/current.bmp
  -> 电子墨水屏刷新显示
```

### 2.2 三端职责

#### 固件端 `firmware/`

当前固件围绕 `firmware/main/main.c` 组织，包含四类核心职责：

- 启动与硬件初始化
  - 初始化 NVS、网络事件循环、AXP2101 供电管理、SD 卡、电子墨水屏缓存
- 首次配网
  - 若 `/sdcard/config.txt` 不存在或无效，则进入 SoftAP 配网模式
  - 创建设备热点 `SmartFrame-xxxx`
  - 启动本地 HTTP 服务，提供扫描 Wi-Fi 和提交凭证能力
- 正常运行
  - 连接 Wi-Fi
  - 根据配置进入 HTTP 轮询模式、直链单图模式或 MQTT 图片通知模式
- 图像显示
  - 下载 BMP 到 `/sdcard/incoming.bmp`
  - 校验 BMP 格式后重命名为 `/sdcard/current.bmp`
  - 通过 `GUI_ReadBmp_RGB_6Color()` + `epaper_port_display()` 刷屏

#### 服务端 `server/`

当前服务端是一个 FastAPI 应用，入口在 `server/main.py`，主要负责：

- 设备注册与绑定码管理
  - `devices.json`：设备注册与绑定关系
  - `bind_codes.json`：动态绑定码状态
- 图片上传与转换
  - 接收小程序上传的原图
  - 使用 `server/image_processor.py` 转为 `800x480`、6 色量化、24-bit BMP
  - 保存到 `server/photos/{device_id}/current.bmp`
- 设备状态查询
  - 基于 `last_seen` 返回在线状态
- MQTT 绑定监听
  - 服务启动后会订阅 `smartframe/bind/request`
  - 用于处理设备侧发起的绑定请求并回 ACK

#### 小程序端 `miniprogram/`

当前小程序结构比较轻量，主要有两个页面：

- `pages/bind/`
  - 输入 6 位绑定码
  - 调用 `/api/bind`
- `pages/index/`
  - 选择图片
  - 调用 `/api/upload`
  - 调用 `/api/status/{device_id}` 查询设备在线状态

## 3. 关键业务流程

### 3.1 首次配网与绑定

当前代码体现的首次使用流程如下：

1. 设备启动，尝试读取 `/sdcard/config.txt`
2. 若配置不存在，进入 SoftAP 配网模式
3. 设备创建热点 `SmartFrame-{device_id后4位}`
4. 用户手机连接热点并访问设备本地网页
5. 网页调用设备内置接口扫描周边 Wi-Fi，并提交 Wi-Fi 与可选 MQTT 参数
6. 设备连接目标 Wi-Fi
7. 设备向服务端申请动态绑定码
8. 墨水屏显示绑定码与设备编号
9. 家人在微信小程序输入绑定码
10. 服务端建立 `openid -> device_id` 绑定关系
11. 后续小程序上传的照片将归属到该设备

### 3.2 日常发图流程

1. 小程序调用 `wx.uploadFile()` 上传图片到 `/api/upload`
2. 服务端根据 `openid` 找到已绑定设备
3. 服务端执行图片缩放、量化、抖动和 BMP 导出
4. 服务端将结果覆盖到对应设备目录下的 `current.bmp`
5. 设备通过以下任一方式感知更新：
   - HTTP 轮询：周期访问 `/api/photo/{device_id}/latest.bmp`
   - MQTT 触发：收到主题消息后再执行下载
6. 固件将 BMP 保存到 SD 卡并刷新电子墨水屏

## 4. 目录结构与职责

```text
smart-frame/
├── docs/                      # 方案文档、问题记录、调研与实施总结
├── firmware/                  # ESP32-S3 固件工程（ESP-IDF）
│   ├── components/
│   │   ├── axp2101_bsp/       # 电源管理芯片初始化
│   │   ├── epaper_port/       # 墨水屏底层端口适配
│   │   ├── epaper_src/        # 墨水屏绘制与 BMP 解码
│   │   ├── mqtt_photo_client/ # MQTT 触发图片下载
│   │   ├── photo_client/      # HTTP 图片下载与 BMP 校验
│   │   ├── sdcard_bsp/        # SD 卡读写
│   │   └── softap_prov/       # SoftAP 配网与本地网页
│   ├── main/
│   │   ├── main.c             # 主流程入口
│   │   ├── frame_config.c/h   # 配置读写与 device_id 生成
│   │   └── wifi_sta.c/h       # Wi-Fi STA 连接
│   └── sdcard/config.txt      # 示例配置文件
├── miniprogram/               # 微信小程序
└── server/                    # FastAPI 后端
```

## 5. 关键配置理解

### 5.1 固件配置文件

固件实际读取的是 `/sdcard/config.txt`，当前代码支持的关键字段包括：

- `wifi_ssid`
- `wifi_password`
- `server_url`
- `device_id`
- `mqtt_broker_url`
- `mqtt_username`
- `mqtt_password`
- `mqtt_topic`
- `mqtt_client_id`
- `poll_interval`
- `mqtt_keepalive_sec`
- `bind_code`
- `bind_status`
- `bind_code_expires`

其中：

- 若存在 `mqtt_broker_url`，设备可进入 MQTT 图片通知模式
- 若存在 `server_url + device_id`，设备可进入 HTTP 轮询模式
- 若仅存在 `server_url` 且 `device_id` 为空，可进入直链单图模式

### 5.2 服务端运行依赖

`server/requirements.txt` 显示服务端依赖较轻：

- `fastapi`
- `uvicorn`
- `python-multipart`
- `Pillow`
- `aiofiles`
- `paho-mqtt`

这说明服务端当前是典型的轻量 Python Web 服务，存储层仍为文件系统 + JSON 文件。

## 6. 当前实现状态判断

基于当前代码，我对项目状态的判断如下。

### 6.1 已形成的完整主链路

以下能力已经在仓库中形成相对完整闭环：

- ESP32-S3 启动、挂载 SD 卡、连接 Wi-Fi、刷新电子墨水屏
- SoftAP 本地网页配网
- 动态绑定码申请与绑定
- 小程序上传图片
- 服务端图片转换与存储
- 固件 HTTP 下载 BMP 并刷新显示

如果只按“家人上传照片 -> 相框周期拉取刷新”这条主链路理解，项目已经具备可运行形态。

### 6.2 已实现但还带有演示性质的部分

以下实现已存在，但明显仍偏 Demo/迭代中状态：

- 小程序 `openid`
  - `pages/bind/bind.js` 中直接把 `wx.login()` 返回的 `code` 当作临时 `openid`
  - 这适合联调，不适合正式生产
- 存储层
  - 服务端仍使用 `devices.json`、`bind_codes.json` 与本地文件夹存储图片
  - 并发、恢复、审计能力有限
- 配置与密钥管理
  - 示例配置文件里包含明文 Wi-Fi/MQTT 参数
  - 更适合本地调试，不适合公开分发

### 6.3 当前代码中的关键注意点

这几条不是“无法运行”的问题，但在理解项目时必须知道：

1. SoftAP 配网阶段申请绑定码的服务端地址是写死的  
   `firmware/main/main.c` 中 `request_bind_code()` 直接请求 `http://1.2.3.4:8000/api/devices/{device_id}/bind-code`，没有复用 `config.txt` 或页面输入的服务端地址。

2. 小程序服务端地址仍是占位符  
   `miniprogram/app.js` 中当前还是 `http://YOUR_SERVER_IP:8000`，部署前必须替换。

3. MQTT 图片链路在仓库内并未完全闭环  
   固件侧已经实现 `mqtt_photo_client`，可以订阅 `device/{device_id}/image` 后触发下载；但当前 `server/main.py` 的 `/api/upload` 只负责保存图片，没有同步发布图片更新 MQTT 消息。  
   这意味着：
   - HTTP 轮询链路是当前仓库里最完整、最直接可运行的主链路
   - MQTT 图片通知能力更像“设备侧已准备好，服务端推送还需补齐或由外部系统承担”

4. 项目文档与当前代码存在轻微代差  
   `docs/README.md` 仍以 HTTP 轮询主线为主，而 2026-04-19 的实施总结文档已经体现了 SoftAP + 动态绑定码改造。理解项目时应以代码为准，文档作辅助。

## 7. 关键源码入口

如果后续要继续接手开发，建议优先从以下文件入手：

- 固件主流程：`firmware/main/main.c`
- 固件配置读写：`firmware/main/frame_config.c`
- HTTP 下载：`firmware/components/photo_client/photo_client.c`
- MQTT 图片通知：`firmware/components/mqtt_photo_client/mqtt_photo_client.c`
- SoftAP 配网：`firmware/components/softap_prov/softap_prov.c`
- 图片转换：`server/image_processor.py`
- 服务端接口：`server/main.py`
- 小程序发图页：`miniprogram/pages/index/index.js`
- 小程序绑定页：`miniprogram/pages/bind/bind.js`

## 8. 运行与调试建议

### 8.1 服务端

在 `server/` 目录：

```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 8.2 固件

在 `firmware/` 目录：

```bash
idf.py set-target esp32s3
idf.py build
idf.py flash monitor
```

### 8.3 小程序

至少要先完成两项基础改动：

- 替换 `miniprogram/app.js` 中的 `serverUrl`
- 按正式环境实现 `code -> openid` 服务端换取逻辑

## 9. 总结

`smart-frame` 当前已经是一个比较清晰的智能相框系统原型，核心价值不在“单个端实现得多复杂”，而在于把硬件设备、云端服务和家属端小程序串成了一个完整业务闭环。

从代码完成度看：

- HTTP 轮询版远程发图链路已经较完整
- SoftAP 配网与动态绑定能力已经接入主流程
- MQTT 图片通知能力已完成设备侧抽象，但服务端推送还没有在当前仓库内完全打通

如果后续继续迭代，最优先的方向通常会是：

- 消除硬编码服务端地址
- 将图片上传后同步发布 MQTT 更新通知
- 把 JSON 文件存储替换为正式数据库
- 完成正式的微信身份体系接入
